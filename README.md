# Simple Password Generator 🚀

A lightweight and easy-to-use Python script that generates strong, random passwords instantly.

## Features
- Fully customizable password length
- Includes uppercase & lowercase letters, numbers, and special characters
- Secure and truly random (uses `random.choices` with `string` constants)
- Simple command-line interface

## Requirements
- Python 3.6 or higher (no external packages needed!)

## How to Use

1. Save the script as `password.py`
2. Run it in your terminal:

```bash
python password.py